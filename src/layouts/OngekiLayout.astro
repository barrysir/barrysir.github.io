---
const { pageTitle } = Astro.props;
import Layout from './Layout.astro';
import Favicon from "@assets/ongeki/favicon.png";
---
<Layout pageTitle={pageTitle} favicon={Favicon.src}>
    <slot />
</Layout>